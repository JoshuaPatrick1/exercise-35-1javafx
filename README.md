# exercise-35-1javafx
A JavaFX application that demonstrates JDBC database connectivity. Features include adding, updating, and displaying records using JavaFX controls (TextField, Button, TableView). All GUI elements are implemented in JavaFX (no Swing/AWT).
